redis:
  address: redis:6379
api_key: ${LIVEKIT_API_KEY}
api_secret: ${LIVEKIT_API_SECRET}
ws_url: ws://livekit:7880
# Traffic never leaves the docker network here — no TLS needed internally.
insecure: true
