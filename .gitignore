# Claude Code / editor runtime state — not part of the project
.claude/
.vscode/
.idea/

# OS cruft
.DS_Store
Thumbs.db

# Root env (Docker Compose) — keep the .example templates tracked
.env
!.env.example
!.env.production.example

# Generated at deploy time by render-prod-conf.sh / init-letsencrypt.sh —
# contain the real domain/keys, regenerate per-environment instead of committing
infrastructure/nginx/prod.conf
infrastructure/livekit/livekit.prod.yaml
infrastructure/livekit/egress.prod.yaml
